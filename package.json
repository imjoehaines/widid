{
  "scripts": {
    "build": "elm-make src/Main.elm --output=temp.js && uglifyjs temp.js --compress --mangle > public/index.js ; rm temp.js",
    "watch:elm": "elm-live src/Main.elm --dir=public --output=public/index.js --debug",
    "watch:node": "nodemon index.js --ignore 'public/*'",
    "watch": "yarn run watch:elm & yarn run watch:node",
    "start": "yarn run build && NODE_ENV=production node index.js"
  },
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "morgan": "^1.8.2",
    "sqlite": "^2.8.0",
    "uglify-js": "^3.0.17"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
