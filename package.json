{
  "scripts": {
    "build": "elm-make src/Main.elm --output=public/index.js",
    "watch": "elm-live src/Main.elm --dir=public --output=public/index.js --open --debug",
    "deploy": "yarn build && surge public/ -d widid.surge.sh"
  },
  "dependencies": {
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "sqlite": "^2.8.0"
  }
}
